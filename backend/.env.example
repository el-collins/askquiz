GROQ_API_KEY=your_groq_api_key_here
DAILY_REQUEST_CAP=50
# Set a real, unique password in production. For local dev this must match
# whatever tests/test_db.py and tests/conftest.py assume (default: askquiz).
POSTGRES_PASSWORD=askquiz
